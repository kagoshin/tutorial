apply plugin: "java"
apply plugin: "compare-gradle-builds"

// 中略

apply plugin: "application"

version = 1.0

mainClassName = "com.example.Hello"

compareGradleBuilds {
  targetBuild {
    gradleVersion "2.0"
  }
}

