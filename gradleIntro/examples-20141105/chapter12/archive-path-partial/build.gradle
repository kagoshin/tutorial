task docsZip(type: Zip) {
  // <プロジェクトディレクトリ>/build/docs-jdk15.zip に出力
  destinationDir = file("build")
  appendix = "docs"
  classifier = "jdk15"

  from ("src/dist") {
    include "**/*.html", "**/*.css", "**/*.js"
  }
}

